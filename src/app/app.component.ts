import { Component } from '@angular/core';

/**
 * The app entrypoint.
 * Displays the main router outlet, dispatching to either the generator or the payments area.
 */
@Component({
    selector: 'aio-app',
    templateUrl: './app.component.html',
    styleUrls: ['./app.component.scss']
})
export class AppComponent {
}
